fastapi
uvicorn[standard]
python-multipart
python-docx
python-pptx
requests
aiofiles
googletrans==4.0.2
# Torch, transformers and sentencepiece are only needed for the
# optional "huggingface" provider.  They are omitted from the
# default dependency set to simplify deployment on free hosts like
# Render (which may not have a Rust toolchain available).
# To enable HuggingFace translation later, install them manually:
#
#   pip install torch transformers sentencepiece

azure-ai-translation-document
azure-storage-blob
azure-core
azure-identity
